# Обзор остальных модулей проекта (ADC, RTC, system, USB, config, main)

Дата обзора: 2026-08-16
Проект: PIC18F46J50_TEST.X

Модули `ringbuf` и `dbio` рассмотрены отдельно — `ringbuf_review.md`, `dbio_review.md`.

Отметка «✅ исправлено» означает, что правка уже внесена в код; «⏸ отложено» —
замечание подтверждено, но правка требует решения по проекту и не вносилась.

---

## 1. ADC.c / ADC.h

### 1.1. ✅ `ADC_stop_IT()` не останавливал опрос, а выводил АЦП из строя

`ADC_stop_IT()` гасил `ADON`, `ADIE` и обнулял `ChanIndx`, но `ADC_ms_cbk`
оставался в списке ms-колбэков (снимать регистрацию `system.c` не умеет).
Через 1 мс колбэк видел `ChanIndx == 0` и снова ставил `ADIE = 1`,
`GODONE = 1` — уже при выключенном `ADON`. Преобразование не запускалось,
прерывания завершения не было, `ChanIndx` навсегда оставался нулём.
Симметрично `ADC_start_IT()` не возвращал `ADON = 1`, поэтому после «стопа»
модуль не поднимался вообще.

**Проявление в проекте:** латентное — вызов `ADC_stop_IT()` в `main.c` стоял
после `while(1)` и был недостижим.

**Сделано:** введён флаг `adc_running`; `ADC_ms_cbk` запускает скан только при
взведённом флаге, `ADC_start_IT()` поднимает `ADON`, `ADC_stop_IT()` снимает
флаг и сбрасывает `GODONE`.

### 1.2. ✅ Гонка за `ADCON0` между основным кодом и таймерным прерыванием

`ADC_cplt_cbk` зарегистрирован низкоприоритетным (`ADIP = 0`), а `ADC_ms_cbk`
вызывается из **высокоприоритетного** обработчика Timer0 (`system.c:55`), то
есть может прервать и его, и основной код. `ADCON0bits.CHS = ...` — это
read-modify-write: если ms-колбэк вклинится между чтением и обратной записью в
`ADC_start_IT()`, он затрёт выставленный колбэком `GODONE`, и сканирование
встанет насовсем.

Внутри цепочки прерываний инвариант «ms-колбэк работает только при
`ChanIndx == 0`» спасал ситуацию, но `ADC_start_IT()`/`ADC_stop_IT()`
вызываются из `main` и защищены не были.

**Сделано:** обе функции обёрнуты в критическую секцию.

### 1.3. ✅ Данные, разделяемые с прерыванием, не были `volatile`

`ChanValue` и `ChanIndx` пишутся в обработчике завершения и читаются из
`ADC_getChan()`. На текущем `-O0` работает, при включении оптимизации
компилятор вправе закэшировать значение в регистре.

**Сделано:** оба объявлены `volatile` и `static`; колбэки тоже стали `static`.

### 1.4. ✅ `ADC_getChan()` не проверял индекс

`ChanValue[16]`, индекс — номер канала от вызывающего, проверки не было.
Плюс безусловный `InterruptEn()` (см. 3.1).

**Сделано:** при `chan >= ADC_CHAN_VALUES` возвращается `-1`; чтение обёрнуто
в критическую секцию с сохранением состояния `GIE`.

### 1.5. ✅ Прочее

- Возвраты `sys_regiter_IRQ_clbk` / `sys_regiter_ms_clbk` игнорировались —
  при исчерпании таблицы модуль молча не работал бы. `ADC_init()` теперь
  возвращает `ADC_INIT_OK` / `ADC_INIT_CBK_ERR`.
- При всех выключенных каналах `CHAN_MAX == 0` давал массив нулевого размера —
  добавлен `#error`.
- Дублирование `ANCON0`/`ANCON1` с `sys_init()` оставлено намеренно: так
  `ADC_init()` самодостаточен и не зависит от порядка инициализации.

---

## 2. RTC.c / RTC.h

### 2.1. ✅ Макросы `BCDtoDEC` / `DECtoBCD` без скобок

```c
#define BCDtoDEC(BCDByte)  (((BCDByte & 0xF0) >> 4) * 10) + (BCDByte & 0x0F)
```

Ни скобок вокруг параметра, ни вокруг всего выражения: `BCDtoDEC(x) * 2`
раскрывалось как `... + (x & 0x0F) * 2`, а `DECtoBCD(a + b)` — как
`((a + b/10) << 4) | ...`. В текущих вызовах не стреляло.

**Сделано:** скобки расставлены.

### 2.2. ✅ `main.c` писал в RTCC десятичные значения вместо BCD

Регистры RTCC хранят BCD, `DECtoBCD` для этого и существует, но при
инициализации даты не использовался. Проходило только потому, что все
константы (0 и 1) в обеих системах совпадают: `DAY = 25` записался бы как
`0x25`, то есть мусор.

**Сделано:** значения проходят через `DECtoBCD()`.

### 2.3. ✅ `read_RTCC()`: лишнее снятие защиты записи и проверка `RTCSYNC` вне секции

`RTCWREN` управляет только записью — для чтения `unlock_RTCC()` не нужен и
лишь открывает окно записи. Кроме того, `while(RTCSYNC)` выполнялся **до**
`InterruptDis()`: между проверкой и собственно чтением могло вклиниться
прерывание и попасть ровно в окно переноса разрядов.

**Сделано:** `unlock_RTCC()` из пути чтения убран, ожидание `RTCSYNC`
перенесено внутрь критической секции.

### 2.4. Раскладка окна `RTCVALH`/`RTCVALL` — ошибок нет

Сверено с даташитом (таблица 17-3 и описание `RTCPTR<1:0>`, стр. 239):
пары полей `--`/YEAR, MONTH/DAY, WEEKDAY/HOURS, MINUTES/SECONDS совпадают с
кодом, а порядок «сначала `RTCVALL`, затем `RTCVALH`» корректен — указатель
декрементируется только при обращении к `RTCVALH`. Инвариант задокументирован
комментарием в коде, чтобы порядок не «поправили» случайно.

---

## 3. system.c / system.h

### 3.1. ✅ Критические секции не вкладывались

`InterruptEn()` безусловно ставит `GIE = 1`, поэтому пара
`InterruptDis()`/`InterruptEn()` включала прерывания даже там, где вызывающий
код их выключил. Проблема сквозная: `dbio`, `ADC`, `RTC`.

**Сделано:** в `system.h` добавлены `CRIT_DECL()` / `CRIT_ENTER()` /
`CRIT_EXIT()`, сохраняющие и восстанавливающие `GIE`. Локальные копии из
`dbio.c` удалены, `ADC.c` и `RTC.c` переведены на них же.

### 3.2. ✅ Дублирующийся `#define U1_BAUDRATE`

Определялся дважды с одинаковым значением (`system.h:8` и `system.h:17`).
Лишнее определение удалено.

### 3.3. ✅ Систематический уход часов 1 мс

**Измерено на плате:** прежний вариант давал 12019.6 такта вместо 12000
(+1637 ppm). Тик переведён на Timer2 с аппаратным автосбросом по PR2:
50 × 16 × 15 = 12000 командных циклов, период не зависит ни от задержки входа
в прерывание, ни от загрузки процессора. Проверено на реальном железе под
нагрузкой: 11999.98 такта, остаточная погрешность −2 ppm (это уход кварца).
Методика, три сравненных реализации и цифры — `timer_1ms_measurements.md`.

Ниже — исходное описание проблемы.



`HighInterrupts_handler` перезагружает Timer0 присваиванием
(`TMR0H = TMR0H_tmp; TMR0L = TMR0L_tmp`), а не добавлением к текущему
значению. Такты, прошедшие от переполнения до входа в обработчик (латентность,
пролог, возможная задержка низкоприоритетным обработчиком), теряются на каждом
тике, и `get_ms()` отстаёт тем сильнее, чем дольше работает система.

**Что подтвердилось:** «наивная» правка (`TMR0 += reload`) на Timer0 с
предделителем действительно не работает — запись в `TMR0` сбрасывает счёт
предделителя (даташит, 12.3). Рабочих решений оказалось два: Timer0 **без**
предделителя с компенсирующей перезагрузкой (точность зависит от подобранной
константы) и Timer2 с аппаратным автосбросом (точен по построению). Внедрён
второй.

**Что верно и проверено:** расчёт периода корректен — 750 тиков при
предделителе 1:16 дают ровно 1 мс при F_OSC = 48 МГц. Беззнаковая арифметика
`get_ms() - t0` корректно переживает переполнение 32-битного счётчика через
49.7 суток, двойное чтение `timestamp` в `get_ms()` тоже сделано правильно.

### 3.4. ✅ Все колбэки вызывались на каждом прерывании

Низкоприоритетный обработчик прогонял весь список зарегистрированных функций
независимо от источника прерывания — четыре колбэка (UART RX, UART TX, ADC,
USB), каждый заново читал свои флаги. По дизассемблеру это обходилось в
~52 такта на каждый «чужой» источник, около 270 тактов ≈ 22 мкс на прерывание
при F_CYC = 12 МГц. Для сравнения: байт на 230400 бод приходит каждые 43 мкс.

**Сделано:** реестр указателей удалён, диспетчер стал статической цепочкой
прямых вызовов с проверкой пары «разрешение + флаг»:

```c
if(PIE1bits.RC1IE && PIR1bits.RC1IF) dbio_rx_isr();
if(PIE1bits.TX1IE && PIR1bits.TX1IF) dbio_tx_isr();
if(PIE1bits.ADIE  && PIR1bits.ADIF)  ADC_isr();
if(PIE2bits.USBIE && PIR2bits.USBIF) USB_isr();
```

Обработчики модулей переименованы (`dbio_rx_isr`, `dbio_tx_isr`, `ADC_isr`,
`USB_isr`), объявлены в заголовках своих модулей, внутренние проверки флагов из
них сняты как дублирующие. Периодические ms-задачи по-прежнему регистрируются
динамически через `sys_register_ms_clbk()` — там перебор списка правомерен.

Замер: **5 тактов** на «чужой» источник вместо ~52. Дополнительно освободилось
5.2 КБ флеша: с таблицей указателей XC8 не мог построить точный граф вызовов и
дублировал под контекст прерывания целые поддеревья (`ringbuf`, USB, `memcpy`)
— 24 продублированных функции против 7 после переделки.

**Подробности, включая промежуточный вариант с таблицей флагов и объяснение,
почему он не дал выигрыша на `-O0`:** `isr_dispatch_notes.md`.

### 3.5. Мелочи

- ✅ Опечатка в публичном API (`sys_regiter_*` вместо `sys_register_*`)
  исправлена: `sys_register_ms_clbk()` переименован, а `sys_regiter_IRQ_clbk()`
  удалён вместе с реестром обработчиков (см. 3.4).
- ⏸ `delay_cyc_ms(ms)` вводит в заблуждение: тело цикла занимает несколько
  тактов на итерацию, поэтому реальная задержка в разы больше заявленной.
- ⏸ `delay_ms()` работает только при включённых прерываниях — внутри
  критической секции даёт вечный цикл.

---

## 4. USB.c / USB.h

Дескрипторы пересчитаны и сходятся: `cfg_desc` = 34 байта (9 + 9 + 9 + 7),
`HID_DESC_OFFSET` = 18, `report_desc` = 63, строковые = 24 и 34. Таблица
ASCII → HID Usage ID проверена выборочно (буквы, цифры, символы через Shift,
Enter, DEL) — ошибок не найдено. Разведение стадий EP0 и таймаут вместо
безусловного `while(UCONbits.SE0)` сделаны правильно.

Замечания мелкие, все ⏸ (модуль **не проверялся на железе**, поэтому правки
без стенда рискуют оказаться вслепую):

1. **Обработка ошибок шины мертва.** `USB_init()` ставит `UEIE = 0x00`, но
   включает `UIEbits.UERRIE = 1`. При нулевом `UEIE` флаг `UERRIF` не
   взводится никогда — ветка `if(UIRbits.UERRIF)` не выполнится ни разу.
2. **`GET_STATUS` игнорирует получателя** — на запрос статуса эндпойнта
   возвращается статус устройства. Хосты это переживают, но формально не по
   спецификации.
3. **`CLEAR_FEATURE(ENDPOINT_HALT)` подтверждается, но не сбрасывает тоггл**
   EP1: после снятия halt'а `kbd_dts` остаётся прежним, и первый отчёт может
   быть отброшен хостом.
4. **Неявная зависимость от порядка байт:** `key` кладётся в двухбайтовую
   ячейку кольца как `uint16_t`, а разбирается сдвигами. На XC8 работает, но
   связь неочевидна — явная пара байт была бы честнее.

Отдельно (из `USB.txt`, остаётся в силе): VID/PID — демонстрационные
идентификаторы Microchip, для изделия нужны свои.

---

## 5. config.h

### 5.1. ✅ Два комментария противоречили самим настройкам

Сверено с даташитом (раздел Special Features, описание конфигурационных бит):

- `XINST = OFF` был подписан «(Enabled)». Бит `XINST`: 0 = расширенный набор
  команд **выключен**. Значение правильное (XC8 требует именно `OFF`) — врал
  только комментарий.
- `T1DIG = ON` был подписан «Secondary Oscillator clock source may not be
  selected». По даташиту 1 = источник **может** быть выбран независимо от
  состояния `T1OSCEN`; в комментарий попало описание нуля.

`LPT1OSC = ON` с комментарием «High-power operation» — проверено, верно
(1 = high-power).

### 5.2. ✅ Общий include-guard

`config.h` занимал generic-имя `XC_HEADER_TEMPLATE_H` из шаблона MPLAB: второй
такой файл в проекте молча погасил бы первый. Переименован в `CONFIG_H`;
заодно поправлены комментарии к `#endif` в `RTC.h` и `system.h`, где стояло то
же шаблонное имя.

---

## 6. main.c

- ✅ Недостижимый код после `while(1)` (`ADC_stop_IT(); return;`) удалён.
- ✅ Инициализация даты переведена на `DECtoBCD()` (см. 2.2).
- ⏸ Возвраты `dbio_init()`, `ADC_init()`, `dbio_putstring()`,
  `USB_kbd_putstring()`, `sys_regiter_ms_clbk()` по-прежнему игнорируются.
  Для демо-цикла это осознанно, но при добавлении реальной логики
  диагностику стоит хотя бы выводить в отладочный порт.

---

## 7. Проверка сборки

XC8 v3.10, `-mcpu=18F46J50`, ключи проекта (`-std=c99 -mwarn=-3 -O0`):

| Сборка | Program | Data |
|--------|---------|------|
| До правок этого обзора | 20771 B (31.7 %) | 1251 B (33.1 %) |
| После правок разделов 1-2, 5-6 | 20891 B (31.9 %) | 1251 B (33.1 %) |
| После переделки диспетчера (3.4) | **16031 B (24.5 %)** | **1152 B (30.5 %)** |

Правки по модулям обошлись в 120 байт флеша при неизменном ОЗУ. Переделка
диспетчера освободила 4.9 КБ флеша и 100 байт ОЗУ относительно исходного
состояния — за счёт того, что без таблицы указателей XC8 строит точный граф
вызовов и перестаёт дублировать функции под контекст прерывания (24
продублированных функции против 7).

Новых предупреждений нет; из имеющихся — только `never called` на публичные
функции, которые демо не использует (`ADC_stop_IT`, счётчики ошибок `dbio`,
`USB_deinit`, `USB_kbd_getleds`), и оставшиеся семь advisory (1510).

---

## 8. Итог

| # | Замечание | Приоритет | Статус |
|---|-----------|-----------|--------|
| 1.1 | `ADC_stop_IT()` выводил АЦП из строя, `ADC_start_IT()` не поднимал `ADON` | Высокий | ✅ |
| 1.2 | Гонка за `ADCON0` с высокоприоритетным ms-колбэком | Высокий | ✅ |
| 1.3 | `ChanValue` / `ChanIndx` не `volatile` | Средний | ✅ |
| 2.1 | Макросы BCD без скобок | Средний | ✅ |
| 2.2 | Запись десятичных значений в BCD-регистры RTCC | Средний | ✅ |
| 3.1 | Невкладываемые критические секции (сквозная) | Средний | ✅ |
| 2.3 | Лишний unlock и проверка `RTCSYNC` вне секции | Средний | ✅ |
| 1.4 | Нет проверки индекса в `ADC_getChan()` | Низкий | ✅ |
| 5.1 | Неверные комментарии к `XINST` и `T1DIG` | Низкий | ✅ |
| 5.2 | Общий include-guard `XC_HEADER_TEMPLATE_H` | Низкий | ✅ |
| 3.4 | Диспетчер вызывал все колбэки подряд | Средний | ✅ |
| 3.5 | Опечатка `sys_regiter_*` | Низкий | ✅ |
| 3.3 | Систематический уход часов Timer0 | Средний | ✅ проверено на плате |
| 4.1-4.4 | Замечания по USB | Низкий | ⏸ нужен стенд |
| 3.5 | `delay_cyc_ms`, `delay_ms` без прерываний | Низкий | ⏸ документация |
